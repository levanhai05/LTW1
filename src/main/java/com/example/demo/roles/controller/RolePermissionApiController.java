package com.example.demo.roles.controller;

/**
 * DISABLED — Đã chuyển toàn bộ sang RoleApiController.
 * Giữ file này để tránh lỗi Git conflict. Không có @RestController.
 */
public class RolePermissionApiController {
    // Dùng PUT /api/roles/{id}/permissions thay thế
}
